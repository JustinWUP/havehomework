<%= 'Welcome ' + current_user.email if current_user %>
<%= current_user ? link_to('Sign Out', destroy_user_session_url, :method => :delete) : link_to('Sign In', new_user_session_url) %>
<br>
<br>
<%= @content.body %>